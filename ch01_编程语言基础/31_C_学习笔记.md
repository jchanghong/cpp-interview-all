## C++学习笔记

> 本节为入门级 C++ 基础教程，覆盖以下知识点。详细讲解请参见本文档中的专题章节：
> - 指针 → 参见 [1.1 指针与引用](#11-指针与引用)
> - 数组 → 参见 [1.2 数组与字符串](#12-数组与字符串)
> - 结构体 → 参见 [1.4 struct 与 union](#14-struct-与-union)
> - 内存分区 → 参见 [1.3 内存管理](#13-内存管理)
> - 引用 → 参见 [1.1 指针与引用](#11-指针与引用)
> - 函数重载/默认参数 → 参见 docs__README.md 及 [2.1 类与对象](#21-类与对象)
>
> ### 关键速查
> - **goto**：无条件跳转，不建议使用
> - **数组名**：是常量（不可赋值），可获取首地址和 sizeof
> - **值传递 vs 地址传递**：值传递不改变实参，地址传递（指针/引用）可改变实参
> - **指针大小**：32位系统 4 字节，64位系统 8 字节
> - **nullptr**：C++11 引入的类型安全的空指针字面量，替代 C 语言中的 NULL（通常是 `(void*)0` 或 `0`）
> - **const 修饰指针**：`const int *p`（指向常量的指针）vs `int * const p`（指针常量）
> - **引用本质**：指针常量 `int* const`，必须初始化，不可改变绑定
> - **内存四区**：代码区（只读共享）、全局区（全局/静态变量/常量）、栈区（局部变量）、堆区（new/delete）
> - **new/delete**：堆区操作，必须配对使用（数组用 `new[]`/`delete[]`）
> - **函数默认参数**：从右向左连续设置，声明和实现只能一处有默认值
> - **函数重载**：同作用域、同名、参数不同（类型/个数/顺序），返回值不参与重载

---
